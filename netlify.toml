[build]
  command = "npm install && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  # Exclure les fichiers compilés du scan des secrets
  SECRETS_SCAN_OMIT_PATHS = ".next/**,.netlify/**,node_modules/**"

