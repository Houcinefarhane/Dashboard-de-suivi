// Setup file for Vitest tests
import { beforeAll, afterAll } from 'vitest'

beforeAll(() => {
  // Setup before all tests
  // NODE_ENV is read-only in Next.js, skip setting it
})

afterAll(() => {
  // Cleanup after all tests
})

